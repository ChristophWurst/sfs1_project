 @{ ViewData["Title"] = "Your Projects"; } @model ProjectManager.Models.ProjectViewModels.ListProjectViewModel

<h1>Your Projects</h1>

<table class="table table-hover">
    <thead>
        <tr>
            <th>#</th>
            <th>Name</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var project in @Model.Projects) {
        <tr>
            <th scope="row">@project.Id</th>
            <td>@project.Name</td>
            <td><a asp-action="Deleteproject" asp-route-id="@project.Id">delete</a></td>
        </tr>
        }
    </tbody>
</table>

<p>
    <a asp-action="AddProject">Add project</a>
</p>